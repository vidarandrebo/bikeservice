name: Tag

on: 
  push:


jobs:
  tag-api:
    name: "Tag"
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        persist-credentials: false

    - name: version
      run: date +%Y%m%d%H%M%S